# data-cats

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-cats` to build the library.

## Running unit tests

Run `nx test data-cats` to execute the unit tests via [Jest](https://jestjs.io).
