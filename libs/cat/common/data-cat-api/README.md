# data-cat-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-cat-api` to build the library.

## Running unit tests

Run `nx test data-cat-api` to execute the unit tests via [Jest](https://jestjs.io).
